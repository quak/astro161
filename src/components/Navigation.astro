---
import { getMenu } from '../lib/api';
const menudata = await getMenu();
const menuitems = menudata[0].menuItems.edges;
---
<div class="fixed hidden">
{menuitems.map((menuitem) => <a href={menuitem.node.path}>{menuitem.node.label}</a>)}
</div>
